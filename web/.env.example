# Added automatically when the Blob store is connected to the Vercel project
# (`vercel blob create-store nct-inventory --access private`). `vercel env pull .env.local` for local use.
BLOB_READ_WRITE_TOKEN=
# Required: the shared password (never commit it). Unset, every request is refused.
INVENTORY_PASSWORD=
